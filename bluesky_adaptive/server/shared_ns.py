shared_ns = {
    # Server parameters are passed as 'unmutable' to the agent function
    "server_parameters": {"depth": 10, "width": 60},
    # Server variables can be changed both by the server and the agent function
    "server_variables": {"x": 100, "y": 900},
}
